#version 330 core
layout (location = 0) in vec3 position;
layout (location = 1) in vec3 normal;
layout (location = 2) in vec2 texCoord;

uniform mat4 projectionMatrix;
uniform mat4 transformationMatrix;

out vec2 interpTC;
out vec3 interpN;

void main(){
	interpN = normal;
	gl_Position = projectionMatrix * transformationMatrix * vec4(position, 1.0); //always vertex data between -1, 1
	
}